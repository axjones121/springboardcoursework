1. const PI = 3.14 // because const make the variable unchangable
 
2. var is scoped to the function only. var can be accessed when hoisted. let is block scoped.
 
3. you can reassign var, but you cannot reassign const. var can be accessed, when hoisted. const is block scoped.
 
4. you can reassign with let, but you can't reassign with const.
 
5. Hoisting is when variables are lifted to the top of the scope they are declared in.